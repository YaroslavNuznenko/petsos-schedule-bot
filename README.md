# PetSOS Schedule Bot

Telegram бот для PetSOS (онлайн екстрена ветеринарна допомога), який дозволяє ветеринарам диктувати свої доступні часові слоти голосом українською мовою.

## Особливості

- 🎤 Транскрипція голосових повідомлень за допомогою OpenAI Whisper (українська мова)
- 🤖 Автоматичне витягування слотів за допомогою LLM зі строгим JSON виводом
- ✅ Інтерактивне підтвердження з inline кнопками
- 💾 Зберігання в SQLite базі даних через Prisma
- 📊 Експорт розкладу у CSV/XLSX формат
- 🌍 Підтримка часового поясу Europe/Kyiv
- 📝 Підтримка текстових повідомлень як альтернатива голосовим
- 🇺🇦 Повна підтримка української мови

## Технологічний стек

- **Node.js 20+**
- **TypeScript**
- **Telegraf** - фреймворк для Telegram ботів
- **OpenAI API** - Whisper (speech-to-text) + LLM для витягування
- **Prisma** - ORM з SQLite
- **Zod** - валідація схем
- **ffmpeg** - конвертація аудіо
- **xlsx** - експорт у Excel формат

## Вимоги

1. **Node.js 20+** встановлено
2. **ffmpeg** встановлено на вашій системі
3. **Telegram Bot Token** - Отримайте від [@BotFather](https://t.me/botfather)
4. **OpenAI API Key** - Отримайте на [OpenAI Platform](https://platform.openai.com/)

## Встановлення

### 1. Встановлення залежностей

```bash
npm install
```

### 2. Встановлення ffmpeg

#### Windows
Завантажте з [ffmpeg.org](https://ffmpeg.org/download.html) або використайте chocolatey:
```bash
choco install ffmpeg
```

#### macOS
```bash
brew install ffmpeg
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt-get update
sudo apt-get install ffmpeg
```

### 3. Налаштування змінних середовища

Створіть файл `.env` в корені проекту:

```env
BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
DATABASE_URL="file:./prisma/dev.db"
TZ=Europe/Kyiv
```

### 4. Налаштування бази даних

Згенеруйте Prisma клієнт та виконайте міграції:

```bash
npm run prisma:generate
npm run prisma:migrate
```

## Запуск бота

### Режим розробки (з hot reload)

```bash
npm run dev
```

### Режим продакшн

```bash
npm run build
npm start
```

## Тестування через Telegram

1. Запустіть бота (див. вище)
2. Відкрийте Telegram та знайдіть вашого бота (використовуючи username від BotFather)
3. Надішліть `/start` для ініціалізації
4. Надішліть голосове повідомлення або текст з вашою доступністю українською мовою

## Команди

- `/start` - Інструкції з використання (українською)
- `/add_slots` - Додати нові слоти доступності (голосом або текстом)
- `/my_slots [YYYY-MM]` - Переглянути ваші слоти за місяць (за замовчуванням поточний місяць)
- `/clear_month [YYYY-MM]` - Очистити всі слоти за місяць (з підтвердженням)
- `/export_month [YYYY-MM]` - Експортувати розклад за місяць у CSV/XLSX

## Приклади голосових команд українською

Бот розуміє природну українську мову:

- "Завтра я доступний з 10 до 13 ургент, і з 15 до 17 ВП"
- "Сьогодні з 9 до 12 тільки ургент"
- "У понеділок з 14 до 18 ВП, у середу з 10 до 15 ургент"
- "Післязавтра з 8 до 12 ургент"
- "У вівторок з 16 до 20 ВП"
- "ВП не беру, тільки ургент з 10 до 14"

📋 **Детальні приклади текстів для копіювання:** Дивіться файл [EXAMPLES_UA.md](./EXAMPLES_UA.md) з повним списком прикладів, які можна копіювати та вставляти після команди `/add_slots`.

## Типи слотів

- **URGENT** - короткі, термінові консультації
- **VP** - вузькопрофільні, розгорнуті консультації

### Правила типів слотів

- Ветеринар може бути доступним для URGENT, але НЕ для VP
- Якщо VP не згадано, за замовчуванням використовується URGENT
- Якщо транскрипт каже, що VP недоступний (наприклад, "ВП не беру", "тільки ургент"), НЕ створюються VP слоти

## Правила слотів

- **Часовий пояс**: Europe/Kyiv
- **Вікно планування**: сьогодні → сьогодні + 31 день
- **Тривалість слота**: 60 хвилин
- **Округлення часу**:
  - Початок → ВНИЗ до найближчої години (10:30 → 10:00)
  - Кінець → ВГОРУ до найближчої години (10:30 → 11:00)
- Якщо кінець ≤ початок → слот відкидається
- Повертаються тільки діапазони (НЕ розбиваються на години)

## Архітектура

```
src/
  bot/
    index.ts          # Точка входу бота, реєстрація команд
    handlers.ts       # Обробники повідомлень та команд
  services/
    telegram.ts       # Завантаження файлів з Telegram
    audio.ts          # Конвертація аудіо (OGG в MP3)
    whisper.ts        # Транскрипція через OpenAI Whisper
    extract.ts        # Витягування слотів через LLM
    db.ts             # Операції з базою даних
  domain/
    schema.ts         # Zod схеми валідації
    normalize.ts      # Нормалізація дат/часу
prisma/
  schema.prisma       # Схема бази даних
```

## Як це працює

1. **Голосовий вхід**: Користувач надсилає голосове повідомлення через Telegram
2. **Завантаження**: Бот завантажує файл `.ogg` з Telegram
3. **Конвертація**: Конвертує `.ogg` в `.mp3` за допомогою ffmpeg
4. **Транскрипція**: Надсилає аудіо до OpenAI Whisper API (з `language: "uk"`)
5. **Витягування**: Надсилає транскрипт до LLM для структурованого витягування
6. **Валідація**: Валідує витягнуті слоти за допомогою Zod
7. **Підтвердження**: Показує слоти користувачу з inline кнопками
8. **Зберігання**: Зберігає підтверджені слоти в SQLite базу даних

## Схема бази даних

- **Vet**: Зберігає інформацію про ветеринара (Telegram user ID, ім'я)
- **AvailabilitySlot**: Зберігає часові слоти (дата, час початку/кінця, тип, джерело)

## Формат експорту

Команда `/export_month` експортує два файли:
- **CSV** з колонками: Дата, Час початку, Час закінчення, Тип
- **XLSX** (Excel) з тими ж колонками

## Приклади українських виразів

Бот коректно розпізнає:

- **Дати**: "сьогодні", "завтра", "післязавтра", "у понеділок", "у вівторок", "у середу"
- **Час**: "з 10 до 13", "з десятої до тринадцятої"
- **Типи**: "тільки ургент", "ВП не беру", "ургент і ВП"

## Вирішення проблем

### Бот не відповідає
- Перевірте, що `BOT_TOKEN` правильний в `.env`
- Переконайтеся, що бот запущено (`npm run dev`)

### Транскрипція не працює
- Перевірте, що `OPENAI_API_KEY` правильний
- Переконайтеся, що у вас є кредити в обліковому записі OpenAI
- Переконайтеся, що аудіо файл не пошкоджений

### Помилки ffmpeg
- Перевірте встановлення ffmpeg: `ffmpeg -version`
- Переконайтеся, що ffmpeg в PATH вашої системи

### Помилки бази даних
- Запустіть `npm run prisma:generate` для регенерації Prisma клієнта
- Запустіть `npm run prisma:migrate` для застосування міграцій

## Локальне тестування через Telegram

1. Створіть бота через [@BotFather](https://t.me/botfather)
2. Отримайте токен бота
3. Додайте токен до `.env` файлу
4. Запустіть бота: `npm run dev`
5. Знайдіть вашого бота в Telegram та надішліть `/start`
6. Надішліть голосове повідомлення українською мовою з вашою доступністю

## Ліцензія

ISC

## Автор

Побудовано для академічного дипломного проекту - PetSOS Schedule Bot MVP
